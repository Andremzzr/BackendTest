<%- include('partials/header') -%>
 

<main>
    <div class="d-flex justify-content-center mt-5 congrats-message">
        Congrats <b> <%= name %></b> , you're in the competition, send this link to your friends, so they could join in
    </div>
    <div class="d-flex justify-content-center mt-5">
       <h2>Link</h2>
     </div>
    <div class="d-flex justify-content-center mt-2">
       <div class="email-container">
          <a href="/form/<%= link %>/signin">localhost:5000/form/<%= link %>/signin </a> 
       </div>
    </div>
    
</main>

<%- include('partials/footer') -%> 